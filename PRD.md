# Porter — PRD (Product Requirements Document)

> 개발자가 로컬 Mac과 SSH 원격 서버에서 실행 중인 개발 프로세스, 포트, 세션을
> 한 화면에서 모니터링하고 안전하게 제어하는 Mac 네이티브 프로세스 매니저.

| 항목 | 내용 |
|---|---|
| 제품명 | Porter (가칭) |
| 플랫폼 | macOS 14+ (Apple Silicon / Intel), SwiftUI 네이티브 |
| 버전 | v0.1 (MVP) |
| 작성일 | 2026-07-10 |

---

## 1. 문제 정의

로컬/원격 개발 중 반복적으로 발생하는 실제 마찰:

1. `localhost:3000`이 이미 점유되어 있는데 **어떤 프로세스인지 매번 찾아야 함**
2. `lsof -i :3000` → `ps aux | grep` → `kill -9` → 재확인… 같은 **명령 조합의 반복**
3. 여러 프로젝트를 동시에 띄우면 **어떤 서버가 살아있는지 헷갈림**
4. 메인 Mac에서 SSH로 원격 개발 머신에 접속해 작업할 때, **원격 프로세스는 같은 방식으로 관리할 수단이 없음**
5. Claude Code / Codex / worker / dev server 등 **장시간 실행 프로세스의 상태 확인이 번거로움**
6. `ps aux` 출력에서 PID를 잘못 읽어 **엉뚱한 프로세스를 죽일 위험**

기존 대체재(Activity Monitor, htop, lsof, tmux, Docker Desktop, Raycast 스크립트)는
"**로컬/원격 개발 프로세스 관리**"라는 목적에 최적화되어 있지 않다.
Activity Monitor는 포트를 모르고, lsof는 UI가 없고, htop은 원격마다 접속해야 한다.

## 2. 타깃 사용자

- **1차**: Mac에서 여러 로컬 서버·worker·DB·AI agent CLI를 동시에 실행하는 풀스택/AI/인프라 개발자
- **초기 페르소나**: "SSH 중심으로 일하는 파워 유저" — 메인 Mac은 컨트롤 타워,
  실제 빌드/서버/AI 작업은 원격 머신(GPU 서버, 홈서버, 클라우드 VM)에서 실행
- **2차**: 팀 리드/CTO, 로컬 개발 환경을 자주 디버깅하는 개발자

## 3. Narrowest Wedge (MVP 집중 범위)

> **"로컬/SSH 머신의 개발 서버와 포트 점유 프로세스를 찾아서 안전하게 종료/재시작하는 앱"**

전체 인프라 대시보드도, 터미널 IDE도 아니다. 포트 → 프로세스 → 제어, 이 흐름 하나에 집중한다.

### MVP Validation Test — "30초 안에"

앱을 열고 30초 안에 아래를 모두 할 수 있으면 MVP 성공:

- [ ] 로컬 또는 SSH 타깃 선택
- [ ] 현재 점유 중인 포트 목록 확인
- [ ] 3000, 5173, 8000, 8080 같은 개발 포트의 프로세스 식별
- [ ] 로그/명령어/작업 디렉토리 확인
- [ ] 안전 확인(confirmation) 후 kill 또는 restart
- [ ] 결과가 즉시 UI에 반영

## 4. 기능 명세

### F1. 타깃 관리 (로컬 + SSH)
- **F1.1** 기본 타깃 "My Mac" (로컬) 상시 제공
- **F1.2** `~/.ssh/config`의 Host 항목 자동 인식 → 사이드바에 SSH 타깃으로 노출 (와일드카드 제외)
- **F1.3** 수동 SSH 타깃 추가 (라벨, host, user, port) — 로컬에 영구 저장
- **F1.4** 타깃별 연결 상태 표시 (연결됨 / 실패 / 확인 중)
- **F1.5** SSH는 시스템 `ssh` 바이너리 + 사용자의 기존 키/agent/config를 그대로 사용.
  비밀번호 저장 없음, BatchMode(키 인증 실패 시 즉시 에러), ControlMaster 연결 재사용

### F2. 포트/프로세스 스캔
- **F2.1** 선택 타깃의 **LISTEN 중인 TCP 포트 전체 목록** (포트, 주소, 프로세스명, PID, 사용자)
- **F2.2** macOS/원격 공통: `lsof` 기반. lsof가 없는 Linux 서버는 `ss -ltnp` 폴백
- **F2.3** 개발 포트 인텔리전스: 3000(Node/Next), 5173(Vite), 8000(Django/FastAPI),
  8888(Jupyter), 11434(Ollama), 5432(Postgres), 6379(Redis) 등 잘 알려진 포트에 라벨 표시
- **F2.4** 자동 새로고침 (기본 5초, 토글 가능) + 수동 새로고침
- **F2.5** 검색/필터: 포트 번호, 프로세스명, PID, 사용자로 즉시 필터.
  포트 번호 검색 결과가 없으면 **"이 포트는 비어 있음"** 을 명시적으로 표시 (포트 충돌 사전 확인 용도)

### F3. 프로세스 상세
- **F3.1** PID, PPID, 사용자, CPU%, MEM%, 시작 시각
- **F3.2** **전체 실행 명령어** (복사 버튼)
- **F3.3** **작업 디렉토리(cwd)** (복사 버튼)
- **F3.4** 프로세스가 열어둔 로그 파일 자동 탐지 (`*.log`, `/logs/` 경로) + tail 미리보기

### F4. 안전한 제어
- **F4.1** Kill (SIGTERM) — 확인 다이얼로그에 프로세스명/PID/명령어/타깃을 명시
- **F4.2** Force Kill (SIGKILL) — SIGTERM 후에도 살아있을 때 2차 수단으로 노출
- **F4.3** Restart — 기록된 cwd에서 동일 명령어를 재실행 (`kill → nohup 재기동`)
- **F4.4** **보호 휴리스틱**: root 소유, 시스템 경로(`/System`, `/usr/libexec` 등), 낮은 PID의
  프로세스는 경고 배지 + 추가 확인 단계 요구. 실수로 시스템 프로세스를 죽이는 사고 방지
- **F4.5** 모든 제어 행위는 Activity Feed에 기록

### F5. Activity Feed
- **F5.1** 스캔/킬/재시작/연결 이벤트를 시간순으로 기록한 하단 피드 (접기 가능)
- **F5.2** 어떤 타깃에서 무엇을 했는지 항상 추적 가능 — "내가 아까 뭘 죽였지?"에 답한다

### F6. CLI 진단 모드
- **F6.1** `porter --scan [ssh-host]` — GUI 없이 동일한 스캔 엔진으로 포트 목록을 터미널에 출력.
  스캔 파이프라인의 헤드리스 검증 및 스크립팅 용도

## 5. UX / 디자인 가이드

[Orca](https://github.com/stablyai/orca) 계열의 어두운 개발자 도구 미학을 따른다.

- **다크 전용** 테마: 딥 네이비-블랙 배경, 저채도 패널, 포인트 컬러는 시그널 그린/블루
- **3-패널 작업 공간**: 좌측 타깃 사이드바 / 중앙 포트·프로세스 리스트 / 우측 상세 인스펙터
- **하단 Activity Feed** 스트립 (터미널 로그 느낌, 모노스페이스)
- 기술 데이터(포트, PID, 명령어)는 **모노스페이스**, 상태는 컬러 닷/칩으로 표현
- 위험 동작(kill)은 레드 계열로 명확히 구분, hover 시에만 노출되는 quick action
- Orca가 "AI Agent Development Environment"라면 Porter는 **"Process & Runtime Development Environment"**

## 6. 아키텍처

```
┌─────────────────────────── Porter.app (SwiftUI) ───────────────────────────┐
│  Sidebar(Targets) │ PortList │ DetailPanel │ ActivityFeed                  │
│                          AppState (@MainActor)                             │
│  ┌──────────────────── Scan/Control Engine ─────────────────────────────┐  │
│  │  Scanner: 스크립트 생성 + lsof -F / ss 파서 (로컬·원격 공통)          │  │
│  │  CommandRunner 프로토콜                                               │  │
│  │    ├─ LocalRunner  → /bin/zsh -c                                      │  │
│  │    └─ SSHRunner    → /usr/bin/ssh (BatchMode, ControlMaster 재사용)   │  │
│  │  SSHConfigParser: ~/.ssh/config Host 자동 인식                        │  │
│  └───────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

핵심 설계 원칙:
- **로컬과 원격은 같은 코드 경로** — 스크립트 생성/파싱은 공유, 실행기만 교체
- **원격 왕복 최소화** — 상세 조회는 마커로 구분된 단일 스크립트 1회 실행
- 서버에 에이전트 설치 불필요 — 표준 유닉스 도구(lsof/ss/ps/kill)만 사용

## 7. 비범위 (v0.1에서 하지 않는 것)

- 내장 터미널 에뮬레이터 / tmux 세션 어태치
- UDP·Docker 컨테이너·launchd 서비스 관리
- 프로세스 그룹/프로젝트 정의 및 일괄 기동 (v0.2 후보)
- 메뉴바 상주 모드, 알림 (v0.2 후보)
- Windows/Linux 클라이언트

## 8. 성공 지표

- 포트 충돌 해결 소요 시간: 터미널 조합 대비 **30초 → 5초**
- 주 3회 이상 재방문하는 활성 사용자 비율
- kill 오조작(의도치 않은 프로세스 종료) 리포트 0건

## 9. 마일스톤

| 버전 | 범위 |
|---|---|
| v0.1 (이번 MVP) | F1–F6 전체, 다크 UI, SPM 빌드 |
| v0.2 | 메뉴바 모드, 프로세스 그룹 재기동, 로그 스트리밍 |
| v0.3 | tmux 세션 뷰, Docker 인식, 앱 번들/서명 배포 |
